1. Home page

    1.1 Code
    Group->checkConfirmationInterval(code)
    Auth->checkPassword(code)
        return registrationData, contest data
    Contests->getData()

    1.2 Guest
    User->createGuest()
        return registrationData
    Contests->getData()


    1.3 Register
    User->createRegular(user data)
        return registrationData
    Contests->getData()


2. Contest

    2.1 Start
    Contest->get(contestID)
        return contest data
    Team->create(contestants array, contestID)
        return teamID, password
    Contest->loadData(teamID)
        return questionsData, scores, answers, isTimed, teamPassword
    Timer->start(teamID)
        return remainingSeconds, ended

    2.2 During contest
    send [teamID, teamPassword, answers] to answers.php on user answer

    2.3 Finish
    Contest->close(teamID, teamPassword)


3. Log out
    Session->destroy()
