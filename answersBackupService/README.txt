
Installation
============

Use ruby 2.0.0p247 and run "bundle install" in this directory to install
the dependencies.

Copy config.yaml.template to config.yaml and edit as needed.

Run ./run to start the webservice.

